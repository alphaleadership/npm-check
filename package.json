{
  "name": "npm-scan",
  "version": "0.1.0",
  "private": true,
  "description": "Monitor npm publishes and flag newly introduced preinstall and postinstall scripts.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node src/main.ts && node count_ultra.js",
    "dev": "node --watch src/main.ts",
    "fetch-packument": "node src/fetch-packument.ts",
    "sync-env": "node src/sync-env.ts",
    "typecheck": "tsc --noEmit",
    "blacklist": "node src/blacklist.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "dotenv": "^17.2.3",
    "libsodium-wrappers": "^0.8.1",
    "piscina": "^4.5.0",
    "semver": "^7.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/semver": "^7.5.8",
    "typescript": "^5.9.3"
  }
}
